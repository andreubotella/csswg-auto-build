<!DOCTYPE html>
<meta charset="UTF-8" />
<!--
  We want the redirect to keep the current URL's fragment, but that can't be
  done if without JS support. So we use a meta refresh inside a noscript for
  browsers that don't support JS, and do a JS-based redirection otherwise.
-->
<noscript>
    <meta http-equiv="refresh" content="0; URL={{ redirect_base }}/{{ spec_folder }}/" />
</noscript>
<title>Redirecting...</title>
<style>
    :root {
        color-scheme: light dark;
    }
</style>
<p>Redirecting to <a href="{{ redirect_base }}/{{ spec_folder }}/">{{ redirect_base }}/{{ spec_folder }}</a>...
</p>

<script>
    // JS-based redirection.
    const url = new URL("{{ redirect_base }}/{{ spec_folder }}/", location.href);
    url.hash = location.hash;
    location.replace(url.href);
</script>